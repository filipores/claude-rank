---
name: rank
description: Show your Claude Code rank dashboard with level, XP, streak, and recent achievements.
disable-model-invocation: true
---
Show the user's current Claude Code rank by running: !`claude-rank dashboard`
If the command fails, tell the user to run `claude-rank sync` first.
